# Supabase (ASTRA)

1. Create a project in the Supabase dashboard.
2. Run `migrations/20260322000000_leads.sql` in the SQL Editor.
3. Copy Project URL + anon public key into `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
4. Never expose the `service_role` key in the frontend.
